# Install

> **Stub** — content to be written.

How to get go-rag: build from source (`CGO_ENABLED=0`), a prebuilt binary, or a package manager. Prerequisites: Go 1.22+ and an optional local Ollama for embeddings.

See the [PRD](../internals/PRD_RAG_Database.md) for the authoritative specification.
