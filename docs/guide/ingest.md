# Ingestion

> **Stub** — content to be written.

Adding documents: `go-rag add`, directory scanning (`scan`), file watching (`watch`), supported formats, and idempotent re-ingestion.

See the [PRD](../internals/PRD_RAG_Database.md) for the authoritative specification.
